// hooks
import { useParams } from 'react-router-dom';
// styles
import './Project.css';

export default function Project() {
    const { id } = useParams();
    return (
        <div>
            
        </div>
    )
}
